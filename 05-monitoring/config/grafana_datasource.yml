apiVersion: 1

datasources:
  - name: PostgresSQL
    uid: postgres
    type: postgres
    access: proxy
    url: host.docker.internal:5432
    database: test
    user: postgres
    secureJsonData:
      password: "example"
    jsonData:
      sslmode: disable